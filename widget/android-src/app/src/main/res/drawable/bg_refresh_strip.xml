<?xml version="1.0" encoding="utf-8"?>
<!-- BSF Solar widget — refresh button strip background. A slightly stronger fill +
     border than the card (bg_widget) so the bottom strip reads as a BUTTON, not a
     label. Colours via @color → auto-themes (light tint / lifted dark fill). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/w_refresh_bg" />
    <corners android:radius="12dp" />
    <stroke android:width="1dp" android:color="@color/w_refresh_border" />
</shape>
