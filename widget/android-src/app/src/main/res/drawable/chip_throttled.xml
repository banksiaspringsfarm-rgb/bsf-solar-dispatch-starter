<?xml version="1.0" encoding="utf-8"?>
<!-- Throttled (curtailment) / stale chip (red pill). Fill/border via @color →
     auto-themes. Shown only when curtailed; also reused for STALE/LOCKOUT. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/chip_throttled_bg" />
    <corners android:radius="100dp" />
    <stroke android:width="1dp" android:color="@color/chip_throttled_border" />
</shape>
