<?xml version="1.0" encoding="utf-8"?>
<!-- DISPATCH health chip (green pill). Fill/border via @color → auto-themes
     (light tint / deep dark fill). Chip text colour set in Java. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/chip_dispatch_bg" />
    <corners android:radius="100dp" />
    <stroke android:width="1dp" android:color="@color/chip_dispatch_border" />
</shape>
